import mim = require("main");
import jquery = require("jquery");
import bootstrap = require("bootstrap");


// to force compiler to include libs in define(...)
var m = mim;
var j = jquery;
var b =bootstrap;
